<form method="post" action="{{ path('artist_type_delete', {'id': artist_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist_type.id) }}">
    <button class="btn bgblured">Delete</button>
</form>
