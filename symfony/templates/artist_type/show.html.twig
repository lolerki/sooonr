{% extends 'base.html.twig' %}

{% block title %}ArtistType{% endblock %}

{% block body %}
<div class="container bordrad10 margtop white">
    <div class="row">
        <div class="col s12 white bordrad10 pad20 margtop">
            <h1 class="titlehun">ArtistType</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ artist_type.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ artist_type.name }}</td>
                    </tr>
                </tbody>
            </table>
            <div class="row"></div>
            <button class="btn btn-lg bgblur">
                <a class="white-text" href="{{ path('artist_type_index') }}">back to list</a>
            </button>
            <button class="btn btn-lg bgblur">
                <a class="white-text" href="{{ path('artist_type_edit', {'id': artist_type.id}) }}">edit</a>
            </button>
            {{ include('artist_type/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
