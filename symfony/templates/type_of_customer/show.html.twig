{% extends 'base.html.twig' %}

{% block title %}TypeOfCustomer{% endblock %}

{% block body %}
    <h1>TypeOfCustomer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_customer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_of_customer.name }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ type_of_customer.createdAt ? type_of_customer.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_customer_index') }}">back to list</a>

    <a href="{{ path('type_of_customer_edit', {'id': type_of_customer.id}) }}">edit</a>

    {{ include('type_of_customer/_delete_form.html.twig') }}
{% endblock %}
