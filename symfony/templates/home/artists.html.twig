{% extends "base.html.twig" %}

{% block title %}Sooonr - Booking{% endblock %}

{% block body %}
    <div class="container center">
        <h1 class="center titlehun white-text">{{ 'bookingartists'|trans }}</h1>
    </div>

    <div class="container">
            <div class="row">
                <div class="col s12">
                    <h2 class='left white-text'>Tous nos artistes</h2>
                </div>
            </div>
            <div class="row">
                <div class="col s12 cards-container">
                    {% for artist in profiles %}
                    <div class="card blue-grey darken-1 hoverable">
                        <div class="card-content white-text">
                            <span class="card-title"></span>
                            <p>{{ artist.about }}</p>
                        </div>
                        <div class="card-action">
                            <a href="{{ path('profile_show', {'id': artist.id}) }}">Détails</a>
                        </div>
                    </div>
                    {% endfor %}

                </div>
            </div>
    </div>

{% endblock %}