{% extends 'base.html.twig' %}

{% block title %}Edit Customers{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col s12 white z-depth-4 bordrad10 pad20 margtop">
                <h1>Edit Customers</h1>

                {{ include('customers/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('customers_index') }}">back to list</a>

                {{ include('customers/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
